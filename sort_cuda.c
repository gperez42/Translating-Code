#include <stdio.h>

int main() {
        int n = arr.length;

        for (int i = 0; i < n - 1; i++) {

            int min_idx = i;

            for (int j = i + 1; j < n; j++) {

                    min_idx = j;

            int temp = arr[i];

            arr[i] = arr[min_idx];

            arr[min_idx] = temp;           

        int[] arr = { 64, 25, 12, 22, 11 };

return 0;
}
